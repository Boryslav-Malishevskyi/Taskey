namespace Model
{
    internal class Task
    {
        internal Task(TaskSettings settings)
        {
            this.settings = settings;
        }

        private TaskSettings settings;
    }
}
